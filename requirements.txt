# Discord Bot Requirements
# Install: pip install -r requirements.txt

# Core
discord.py>=2.3.0,<3.0.0
python-dotenv>=1.0.0

# HTTP/API
aiohttp>=3.9.0
aiofiles>=23.0.0

# System monitoring
psutil>=5.9.0

# Database (F28: SQLite Migration)
aiosqlite>=0.19.0

# Data handling (included in stdlib but listed for clarity)
# json, asyncio, pathlib, datetime - stdlib

# Optional: OneDrive backup (needs rclone installed separately)
# rclone: sudo apt install rclone && rclone config

# Optional: Email notifications
# Uses stdlib smtplib - no extra packages needed

# Web Dashboard (Phase 13)
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
PyJWT>=2.8.0
bcrypt>=4.1.0
jinja2>=3.1.0
python-multipart>=0.0.7
itsdangerous>=2.1.0

# Optional: Deep savegame analysis (building counts, power stats)
# pip install satisfactory-save --break-system-packages
satisfactory-save>=0.1.0

# Optional: Minecraft NBT-Parsing (MC savegame analysis)
nbtlib>=2.0.0

# Optional: TeamSpeak 3 Server Query
# ts3>=2.0.0  # Etappe 3.7 (2026-05-01): TS-Server inactive/not-installed, Paket deaktiviert.
# Reaktivieren bei TS-Wiederbelebung (oder Migration zu pyTS3 / python-telnet).
